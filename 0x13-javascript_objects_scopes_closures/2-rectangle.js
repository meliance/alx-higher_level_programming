#!/usr/bin/node

module.exports = class Rectsngle {
        constructor(w, h) {
		if (w <= 0 || h <= 0 || isNaN(w) || isNaN(h) {
			return 0;
		} else {
			this.width = w;
			this.height = h;
		}
        }
};
